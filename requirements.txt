# NakanosimaProject_HumanDetect_2026 runtime dependencies
#
# Target checked on 2026-08-30:
#   Windows 64-bit / Python 3.13 / NVIDIA GeForce RTX 4050
#   NVIDIA driver: CUDA 13.1 compatible, CUDA Toolkit: 12.9
#
# PyTorch wheels bundle their CUDA runtime; the locally installed CUDA Toolkit is
# not used for normal execution.  cu129 was selected to match this machine and
# is supported by the installed driver.
--extra-index-url https://download.pytorch.org/whl/cu129
torch==2.8.0+cu129
torchvision==0.23.0+cu129

# Direct dependencies imported by this project.
numpy>=2.1,<3
opencv-python>=4.11,<6
Pillow>=10,<13
PyYAML>=6,<7
ultralytics>=8.3,<9

# OSNet ReID.  Version 0.2.5 provides the ``torchreid.reid.utils`` import path
# used by human_detector.py.
torchreid==0.2.5

# Runtime dependencies used by Torchreid/OSNet.  Development-only tools are
# intentionally excluded.
Cython>=3,<4
gdown>=5,<6
h5py>=3,<4
imageio>=2,<3
matplotlib>=3.8,<4
scipy>=1.13,<2
six>=1.16,<2
yacs>=0.1.8,<0.2
